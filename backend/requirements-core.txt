# Core backend dependencies (installed now).
# The heavy ML stack (torch, torchvision, monai, transformers, huggingface-hub)
# is deferred to a later phase — see requirements.txt for the full list.
#
# Django pinned to 3.2.25 (LTS): djongo 1.3.6 is incompatible with Django 4.x.
Django==3.2.25
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
drf-spectacular==0.29.0
djongo==1.3.6
pymongo==3.12.3
sqlparse==0.2.4
python-decouple==3.8
Pillow==10.2.0
numpy==1.26.4
scipy==1.11.4
pydicom==2.4.4
nibabel==5.2.0
wfdb==4.1.2
neurokit2==0.2.7
reportlab==4.0.9
# celery / redis removed — there is no async task layer using them. For a shared
# DRF-throttle cache in production, set REDIS_URL and `pip install django-redis`
# (see core/settings.py).
